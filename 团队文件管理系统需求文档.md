# 局域网团队文件管理系统需求文档

**版本号**：V2.0  
**编制日期**：2026年4月30日  
**文档状态**：修订版  
**适用范围**：局域网内小型团队文件管理

---

## 一、项目概述

### 1.1 项目背景

随着团队日常工作中产生的电子文件数量急剧增加，包括图片、Word文档、Excel表格、PDF文件等，传统的文件夹层级管理方式已无法满足高效检索的需求。团队经常面临"知道文件存在，但不知道放在哪个文件夹"的问题，导致大量时间浪费在文件查找上。

本系统部署在局域网环境中，专为小型团队设计，无需复杂的注册流程，通过管理员统一创建账号，实现文件的高效管理和共享。

### 1.2 项目目标

开发一个基于标签的局域网团队文件管理系统，通过"标签+搜索"的方式替代传统的纯文件夹管理，实现文件的快速上传、智能分类和精准检索，大幅提升文件管理和查找效率。

### 1.3 目标用户

- **管理员**：负责系统管理、账号创建、团队空间配置
- **正式用户**：团队成员，拥有个人空间和团队共享空间访问权限
- **访客用户**：仅能查看和预览团队共享空间文件

---

## 二、功能需求

### 2.1 用户管理模块

#### 2.1.1 登录功能

- 系统初始内置一个**管理员账户**（默认用户名：admin，默认密码：admin123，首次登录强制修改密码）
- 支持用户名+密码登录
- 支持"记住我"功能（7天内免登录）
- 登录失败5次后锁定账户10分钟
- 显示当前登录用户信息和权限类型

#### 2.1.2 管理员账号管理

- **创建账号**：
  - 管理员可以创建正式账号和访客账号
  - 创建时需设置：用户名、密码、用户类型（正式/访客）、备注信息
  - 用户名唯一，不可重复
- **账号管理**：
  - 查看所有账号列表（显示：用户名、类型、创建时间、最后登录时间、状态）
  - 重置用户密码
  - 启用/禁用账号
  - 删除账号（删除账号时，其个人空间文件处理方式可选：转移给管理员 / 彻底删除）
  - 修改用户类型（正式 ↔ 访客）

#### 2.1.3 个人信息管理

- 用户可以修改个人头像、昵称
- 用户可以修改登录密码
- 用户可以查看账户存储空间使用情况（仅正式用户）

### 2.2 文件管理模块

#### 2.2.1 文件上传

- 支持拖拽上传和点击上传两种方式
- 支持批量上传多个文件
- 上传时可以为每个文件添加多个标签，批量上传时可选择为文件统一添加标签和逐个添加标签
- **上传时选择存储位置**：
  - 个人空间：仅上传者和管理员可访问
  - 团队共享空间：所有团队成员可访问（访客仅可查看）
- 支持的文件类型：
  - 图片：JPG、PNG、GIF、BMP、WEBP
  - 文档：DOC、DOCX、XLS、XLSX、PPT、PPTX、PDF
  - 其他：TXT、ZIP、RAR
- 显示上传进度条和上传速度
- 支持断点续传
- 上传过程中可以取消上传

#### 2.2.2 文件预览

- 支持在线预览所有支持的文件类型
- 图片预览：支持放大、缩小、旋转、幻灯片播放
- PDF预览：支持翻页、缩放、搜索文本
- Office文档预览：支持基本的内容查看
- 支持全屏预览模式

#### 2.2.3 文件下载

- 支持单个文件下载
- 支持多个文件打包下载为ZIP格式
- 显示下载进度

#### 2.2.4 文件操作

- **个人空间文件操作**：
  - 重命名文件
  - 删除文件（移入回收站）
  - 复制文件
  - 移动文件（可选文件夹）
  - **移动到团队共享空间**：将个人文件移动到团队空间
- **团队共享空间文件操作**：
  - 正式用户：下载、预览、添加标签、评论
  - 管理员：拥有全部操作权限（重命名、删除、移动等）
  - 访客用户：仅可预览和下载

#### 2.2.5 文件夹管理

- 创建、重命名、删除文件夹
- 文件夹层级结构展示
- 支持在文件夹之间移动文件
- 支持文件夹批量操作
- 个人空间和团队空间的文件夹分开显示

### 2.3 标签管理模块

#### 2.3.1 标签创建与管理

- 创建标签可以分为两种方式，提前在标签管理中创建标签和上传文件时候创建标签
- 用户可以创建自定义标签
- 支持为标签设置不同颜色，便于区分
- 支持编辑标签名称和颜色
- 支持删除标签（已经绑定该标签的文件上的标签不消失，但用户的标签管理界面不再有该标签，也就是弱删除），删除后的标签在系统中依旧存在，创建标签时的推荐列表中，依旧显示
- 用户创建的标签，其实都是可以公用的，只是创建它的用户，可以在自己的标签管理界面看到，其他人只有在创建新标签、向共享空间传文件时才会被推荐
- 若用户在创建新标签或者向个人空间传文件并创建新标签时，选择了被系统推荐的其他用户已经创建的标签，则该用户的标签管理界面也能看到该标签
- 显示每个标签关联的文件数量，分私有文件数量和公共文件数量
- 用户可以在上传文件的时候直接创建标签并关联文件
- 为防止用户创建过多重复标签，每次创建新标签，输入标签名时，展示相似的标签
- 标签系统没必要和用户强绑定，该项目是为了方便团队内进行资源共享和交流，以及相关文件、资源的查找

#### 2.3.2 文件打标签

- 上传文件时可以立即添加标签
- 支持为单个文件添加多个标签（无数量限制）
- 支持批量为多个文件添加/移除标签
- 支持从标签建议列表中选择已有标签
- 支持自动补全标签名称
- 公共空间内的文件，每个用户都能补充标签，但只有管理员能删除该文件上已关联的标签

#### 2.3.3 标签分类

- 用户标签管理界面的标签，支持该用户在自己的空间创建标签组，将相关标签归类
- 例如："工作"标签组下可以包含"项目A"、"会议"、"报告"等标签

### 2.4 搜索模块

#### 2.4.1 基础搜索

- 支持按文件名搜索
- 支持按文件内容搜索（PDF、Word、Excel、TXT）
- 支持按文件类型筛选
- 支持按上传时间筛选
- 支持按文件大小筛选
- **支持选择搜索范围**：个人空间 / 团队共享空间 / 全部

#### 2.4.2 标签搜索

- 支持按单个标签搜索
- 支持按多个标签组合搜索（AND/OR逻辑）
  - AND：同时包含所有选中标签的文件
  - OR：包含任意一个选中标签的文件
- 支持在搜索结果中进一步筛选标签

#### 2.4.3 智能搜索

- **相似文件推荐**：
  - 搜索结果中显示"相似文件"推荐
  - 基于文件名相似度、标签相似度推荐
  - 点击"找相似"按钮，查找与当前文件相似的其他文件
- **搜索建议**：
  - 输入搜索关键词时，实时显示相关的搜索建议
  - 建议包括：相关文件名、相关标签、相关文件类型
- **搜索历史**：
  - 保存用户最近的搜索记录
  - 支持快速重复搜索
  - 支持清除搜索历史

#### 2.4.4 筛选功能

- 支持组合多种筛选条件：标签、文件类型、上传时间范围

#### 2.4.5 其他搜索功能

- 支持保存常用搜索条件为"搜索模板"
- 支持搜索历史记录，可快速重复搜索

#### 2.4.6 搜索结果展示

- 支持列表视图和网格视图切换
- 列表视图显示：文件名、文件类型、大小、上传时间、标签、存储位置（个人、公共）
- 网格视图显示：文件缩略图、文件名
- 搜索结果按相关度排序，也可按时间、大小、名称排序
- 高亮显示搜索关键词

### 2.5 回收站模块

- 显示所有已删除的文件和文件夹
- 支持恢复已删除的文件/文件夹到原位置
- 支持永久删除文件/文件夹
- 支持清空回收站
- 回收站文件保留30天，到期自动永久删除
- 个人空间和团队空间的回收站分开管理

### 2.6 统计与概览

- 首页显示存储空间使用情况（个人空间 + 团队空间）
- 显示文件类型分布统计（饼图）
- 显示最近上传的文件列表
- 显示常用标签列表
- 显示最近搜索记录
- 管理员可查看团队整体使用统计

### 2.7 大模型API对接模块

#### 2.7.1 API配置（可选功能）

- **API配置**：
  - 管理员可配置团队通用的大模型API
  - 用户可自行更改、配置大模型API（如OpenAI、Claude、国内大模型等）支持本地部署的大模型
  - 配置项：API类型、API Key、API Base URL、模型名称
  - 支持测试API连接
  - 配置信息加密存储，仅用户本人可见

#### 2.7.2 AI辅助功能（配置API后可用）

- **文件内容摘要**：
  - 用户可以引用文件给AI，让其生成内容摘要
  - 支持Word、PDF、TXT等文档
- **智能问答**：
  - 用户可以引用的个人空间/团队空间的文件，对AI进行问答
  - 支持多轮对话
  - 大模型拥有查询数据库的能力，例如在对话中，用户询问目前系统中的标签总数，则AI可以编写相关搜索语句，去数据库查找。但进行该操作之前要先交给用户确认，用户可以取消

---

## 三、非功能需求（修订）

### 3.1 性能需求

- 单文件上传最大支持5GB（局域网环境）
- 批量上传支持同时上传20个文件
- 文件预览加载时间不超过2秒
- 搜索响应时间不超过1秒
- 系统支持同时在线用户数不少于20人

### 3.2 安全性需求

- 用户密码采用加密存储（BCrypt算法）
- 管理员可以查看系统操作日志
- 每个用户只能访问自己权限范围内的文件
- API Key加密存储，仅用户本人可见
- 定期自动备份用户数据（可配置备份路径）

### 3.3 可用性需求

- 界面简洁直观，操作符合用户习惯
- 支持响应式设计，适配电脑
- 系统平均无故障时间(MTBF)不低于99.9%
- 提供详细的使用帮助文档
- 支持一键备份和恢复系统数据

### 3.4 兼容性需求

- 支持主流浏览器：Chrome、Firefox、Edge、Safari
- 支持Windows、macOS、Linux操作系统
- 支持局域网内IP访问
- 支持自定义端口配置

---

## 四、系统架构

### 4.1 技术栈选型（供参考，可根据实际修改）

- **前端**：Vue 3 + Element Plus + Axios
- **后端**：Spring Boot 3.x + Spring Security + MyBatis-Plus
- **数据库**：MySQL 8.4 + Redis
- **文件存储**：本地文件系统（局域网共享存储）
- **全文检索**：Elasticsearch
- **AI对接**：LangChain4j（支持多种大模型）
- **部署**：Docker + Docker Compose（支持一键部署），最好能支持windows系统部署



&nbsp;